# GitHub Pages deployment has been retired.
# Production hosts (pcln.top / auth.pcln.top) are on Cloudflare Pages.
# See: .github/workflows/cloudflare-pages.yml
name: Deploy GitHub Pages (retired)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type RETIRED to no-op (deployment moved to Cloudflare Pages)"
        required: true
        type: string

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: |
          echo "GitHub Pages deploy is retired. Use cloudflare-pages.yml."
          if [ "${{ inputs.confirm }}" != "RETIRED" ]; then
            exit 1
          fi
